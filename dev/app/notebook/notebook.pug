include ../_mixins
section.d-none#notebook(data-bind="fadeVisible: api_token() && state()=='notebook'")
  .container-fluid
    nav.nav.d-flex.justify-content-between.align-items-center
      h1.navbar-brand.text-shadow #[+modelIcon('notebook')] Notebook