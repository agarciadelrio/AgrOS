include ../_mixins
section.d-none#parcel(data-bind="fadeVisible: state()=='parcel'")
  .container-fluid
    nav.nav.d-flex.justify-content-between.align-items-center
      h1.navbar-brand.text-shadow #[+modelIcon('parcel')] Parcels
      .nav-item.dropdown(data-bind="visible: Parcel.someSelectedParcel")
        a.nav-link.dropdown-toggle(href="#" data-bs-toggle="dropdown") #[i.fa.fa-wrench(aria-hidden='true')] Actions
        ul.dropdown-menu
          li: a.dropdown-item(href="#duplicate") Duplicate
          li: a.dropdown-item(data-bind="click: Parcel.deleteSelected" href="#delete") Delete
      .nav-item
        button.btn.btn-primary.shadow(data-bs-toggle="modal" data-bs-target="#parcelModal" data-bind="click: Parcel.newParcel") #[i.fa.fa-plus] Add Parcel
  .container-fluid
    .table-responsive.table-wrapper.rounded
      table.table-list.table.table-striped.table-hover
        thead
          tr
            th.cursor-pointer(data-bind="click: Parcel.deselectAll") #
            th name
            th active
            th.text-end: +tableNav()
        tbody(data-bind="foreach: parcels")
          tr
            td.id
              input(type="checkbox" data-bind="checked: selected")
            td.name.text-primary(data-bs-toggle="modal" data-bs-target="#parcelModal" data-bind="click: select.bind($parent), text: name")
            td.active(colspan="100%" data-bind="text: active") active
            //-td.text-end
            //-  button.btn.btn-sm.btn-link(data-bs-toggle="modal" data-bs-target="#parcelModal" data-bind="click: select.bind($parent)") edit
include parcel_form
