include ../_mixins
section.d-none#permission(data-bind="fadeVisible: api_token() && state()=='permission'")
  .container-fluid
    nav.nav.d-flex.justify-content-between.align-items-center
      h1.navbar-brand.text-shadow #[+modelIcon('permission')] Permissions
      .nav-item.dropdown(data-bind="visible: Permission.someSelectedPermission")
        a.nav-link.dropdown-toggle(href="#" data-bs-toggle="dropdown") #[i.fa.fa-wrench(aria-hidden='true')] Actions
        ul.dropdown-menu
          li: a.dropdown-item(href="#duplicate") Duplicate
          li: a.dropdown-item(data-bind="click: Permission.deleteSelected" href="#delete") Delete
      .nav-item
        button.btn.btn-primary.shadow(data-bs-toggle="modal" data-bs-target="#permissionModal" data-bind="click: Permission.newPermission") #[i.fa.fa-plus] Add Permission
  .container-fluid
    .table-responsive.table-wrapper.rounded
      table.table-list.table.table-striped.table-hover
        thead
          tr
            th.cursor-pointer(data-bind="click: Permission.deselectAll") #
            th name
            th.text-end: +tableNav()
        tbody(data-bind="foreach: permissions")
          tr
            td.id
              input(type="checkbox" data-bind="checked: selected")
            td.name.w-100(colspan="100%" data-bs-toggle="modal" data-bs-target="#permissionModal" data-bind="click: select.bind($parent), text: name")
            //-td.text-end
            //-  button.btn.btn-sm.btn-link(data-bs-toggle="modal" data-bs-target="#permissionModal" data-bind="click: select.bind($parent)") edit
include permission_form
