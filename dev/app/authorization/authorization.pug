include ../_mixins
section.d-none#authorization(data-bind="fadeVisible: api_token() && state()=='authorization'")
  .container-fluid
    nav.nav.d-flex.justify-content-between.align-items-center
      h1.navbar-brand.text-shadow #[+modelIcon('authorization')] Authorizations
      .nav-item.dropdown(data-bind="visible: Authorization.someSelectedAuthorization")
        a.nav-link.dropdown-toggle(href="#" data-bs-toggle="dropdown") #[i.fa.fa-wrench(aria-hidden='true')] Actions
        ul.dropdown-menu
          li: a.dropdown-item(href="#duplicate") Duplicate
          li: a.dropdown-item(data-bind="click: Authorization.deleteSelected" href="#delete") Delete
      .nav-item
        button.btn.btn-primary.shadow(data-bs-toggle="modal" data-bs-target="#authorizationModal" data-bind="click: Authorization.newAuthorization") #[i.fa.fa-plus] Add Authorization
  .container-fluid
    .table-responsive.table-wrapper.rounded
      table.table-list.table.table-striped.table-hover
        thead
          tr
            th.cursor-pointer(data-bind="click: Authorization.deselectAll") #
            th name
            th.text-end: +tableNav()
        tbody(data-bind="foreach: authorizations")
          tr
            td.id
              input(type="checkbox" data-bind="checked: selected")
            td.name.w-100(colspan="100%" data-bs-toggle="modal" data-bs-target="#authorizationModal" data-bind="click: select.bind($parent), text: name")
            //-td.text-end
            //-  button.btn.btn-sm.btn-link(data-bs-toggle="modal" data-bs-target="#authorizationModal" data-bind="click: select.bind($parent)") edit
include authorization_form
