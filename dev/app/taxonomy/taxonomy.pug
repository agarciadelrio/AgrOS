include ../_mixins
section.d-none#taxonomy(data-bind="fadeVisible: api_token() && state()=='taxonomy'")
  .container-fluid
    nav.nav.d-flex.justify-content-between.align-items-center
      h1.navbar-brand.text-shadow #[+modelIcon('taxonomy')] Taxonomies
      .nav-item.dropdown(data-bind="visible: Taxonomy.someSelectedTaxonomy")
        a.nav-link.dropdown-toggle(href="#" data-bs-toggle="dropdown") #[i.fa.fa-wrench(aria-hidden='true')] Actions
        ul.dropdown-menu
          li: a.dropdown-item(href="#duplicate") Duplicate
          li: a.dropdown-item(data-bind="click: Taxonomy.deleteSelected" href="#delete") Delete
      .nav-item
        button.btn.btn-primary.shadow(data-bs-toggle="modal" data-bs-target="#taxonomyModal" data-bind="click: Taxonomy.newTaxonomy") #[i.fa.fa-plus] Add Taxonomy
  .container-fluid
    .table-responsive.table-wrapper.rounded
      table.table-list.table.table-striped.table-hover
        thead
          tr
            th.cursor-pointer(data-bind="click: Taxonomy.deselectAll") #
            th name
            th.text-end: +tableNav()
        tbody(data-bind="foreach: taxonomies")
          tr
            td.id
              input(type="checkbox" data-bind="checked: selected")
            td.name.w-100(colspan="100%" data-bs-toggle="modal" data-bs-target="#taxonomyModal" data-bind="click: select.bind($parent), text: name")
            //-td.text-end
            //-  button.btn.btn-sm.btn-link(data-bs-toggle="modal" data-bs-target="#taxonomyModal" data-bind="click: select.bind($parent)") edit
include taxonomy_form
